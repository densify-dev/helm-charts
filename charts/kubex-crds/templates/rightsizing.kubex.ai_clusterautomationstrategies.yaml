---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clusterautomationstrategies.rightsizing.kubex.ai
spec:
  group: rightsizing.kubex.ai
  names:
    kind: ClusterAutomationStrategy
    listKind: ClusterAutomationStrategyList
    plural: clusterautomationstrategies
    singular: clusterautomationstrategy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterAutomationStrategy is the Schema for the clusterautomationstrategies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterAutomationStrategy
            properties:
              enablement:
                default: {}
                description: |-
                  enablement toggles which actions are permitted.
                  Defaults are applied at the leaf fields; the section itself defaults to an empty object to allow defaults to propagate.
                properties:
                  cpu:
                    default: {}
                    description: cpu controls enablement rules for CPU resources.
                    properties:
                      limits:
                        default: {}
                        description: limits controls enablement for CPU limits.
                        properties:
                          ceiling:
                            description: ceiling is the maximum allowed resource quantity
                              for this usage.
                            type: string
                          containers:
                            additionalProperties:
                              properties:
                                ceiling:
                                  description: ceiling is the maximum allowed resource
                                    quantity for this usage.
                                  type: string
                                floor:
                                  description: floor is the minimum allowed resource
                                    quantity for this usage.
                                  type: string
                              type: object
                            description: |-
                              containers maps exact container names to floor/ceiling overrides for this usage.
                              Any bound omitted for a specific container falls back to the usage-level floor/ceiling.
                            type: object
                          downsize:
                            default: true
                            description: downsize controls whether resource downsizing
                              actions are permitted.
                            type: boolean
                          floor:
                            description: floor is the minimum allowed resource quantity
                              for this usage.
                            type: string
                          setFromUnspecified:
                            default: false
                            description: setFromUnspecified controls whether an unspecified
                              resource value may be set.
                            type: boolean
                          upsize:
                            default: true
                            description: upsize controls whether resource upsizing
                              actions are permitted.
                            type: boolean
                        type: object
                        x-kubernetes-validations:
                        - message: floor must be less than or equal to ceiling
                          rule: '!has(self.floor) || !has(self.ceiling) || quantity(self.floor).compareTo(quantity(self.ceiling))
                            <= 0'
                      requests:
                        default: {}
                        description: requests controls enablement for CPU requests.
                        properties:
                          ceiling:
                            description: ceiling is the maximum allowed resource quantity
                              for this usage.
                            type: string
                          containers:
                            additionalProperties:
                              properties:
                                ceiling:
                                  description: ceiling is the maximum allowed resource
                                    quantity for this usage.
                                  type: string
                                floor:
                                  description: floor is the minimum allowed resource
                                    quantity for this usage.
                                  type: string
                              type: object
                            description: |-
                              containers maps exact container names to floor/ceiling overrides for this usage.
                              Any bound omitted for a specific container falls back to the usage-level floor/ceiling.
                            type: object
                          downsize:
                            default: true
                            description: downsize controls whether resource downsizing
                              actions are permitted.
                            type: boolean
                          floor:
                            description: floor is the minimum allowed resource quantity
                              for this usage.
                            type: string
                          setFromUnspecified:
                            default: true
                            description: setFromUnspecified controls whether an unspecified
                              resource value may be set.
                            type: boolean
                          upsize:
                            default: true
                            description: upsize controls whether resource upsizing
                              actions are permitted.
                            type: boolean
                        type: object
                        x-kubernetes-validations:
                        - message: floor must be less than or equal to ceiling
                          rule: '!has(self.floor) || !has(self.ceiling) || quantity(self.floor).compareTo(quantity(self.ceiling))
                            <= 0'
                    type: object
                  memory:
                    default: {}
                    description: memory controls enablement rules for memory resources.
                    properties:
                      limits:
                        default: {}
                        description: limits controls enablement for limits.
                        properties:
                          ceiling:
                            description: ceiling is the maximum allowed resource quantity
                              for this usage.
                            type: string
                          containers:
                            additionalProperties:
                              properties:
                                ceiling:
                                  description: ceiling is the maximum allowed resource
                                    quantity for this usage.
                                  type: string
                                floor:
                                  description: floor is the minimum allowed resource
                                    quantity for this usage.
                                  type: string
                              type: object
                            description: |-
                              containers maps exact container names to floor/ceiling overrides for this usage.
                              Any bound omitted for a specific container falls back to the usage-level floor/ceiling.
                            type: object
                          downsize:
                            default: true
                            description: downsize controls whether resource downsizing
                              actions are permitted.
                            type: boolean
                          floor:
                            description: floor is the minimum allowed resource quantity
                              for this usage.
                            type: string
                          setFromUnspecified:
                            default: true
                            description: setFromUnspecified controls whether an unspecified
                              resource value may be set.
                            type: boolean
                          upsize:
                            default: true
                            description: upsize controls whether resource upsizing
                              actions are permitted.
                            type: boolean
                        type: object
                        x-kubernetes-validations:
                        - message: floor must be less than or equal to ceiling
                          rule: '!has(self.floor) || !has(self.ceiling) || quantity(self.floor).compareTo(quantity(self.ceiling))
                            <= 0'
                      requests:
                        default: {}
                        description: requests controls enablement for requests.
                        properties:
                          ceiling:
                            description: ceiling is the maximum allowed resource quantity
                              for this usage.
                            type: string
                          containers:
                            additionalProperties:
                              properties:
                                ceiling:
                                  description: ceiling is the maximum allowed resource
                                    quantity for this usage.
                                  type: string
                                floor:
                                  description: floor is the minimum allowed resource
                                    quantity for this usage.
                                  type: string
                              type: object
                            description: |-
                              containers maps exact container names to floor/ceiling overrides for this usage.
                              Any bound omitted for a specific container falls back to the usage-level floor/ceiling.
                            type: object
                          downsize:
                            default: true
                            description: downsize controls whether resource downsizing
                              actions are permitted.
                            type: boolean
                          floor:
                            description: floor is the minimum allowed resource quantity
                              for this usage.
                            type: string
                          setFromUnspecified:
                            default: true
                            description: setFromUnspecified controls whether an unspecified
                              resource value may be set.
                            type: boolean
                          upsize:
                            default: true
                            description: upsize controls whether resource upsizing
                              actions are permitted.
                            type: boolean
                        type: object
                        x-kubernetes-validations:
                        - message: floor must be less than or equal to ceiling
                          rule: '!has(self.floor) || !has(self.ceiling) || quantity(self.floor).compareTo(quantity(self.ceiling))
                            <= 0'
                    type: object
                type: object
              inPlaceResize:
                default: {}
                description: inPlaceResize configures in-place resize behavior.
                properties:
                  containerRestart:
                    default: false
                    description: containerRestart controls whether in-place resize
                      is allowed to restart containers.
                    type: boolean
                  enabled:
                    default: true
                    description: enabled controls whether in-place resize is allowed.
                    type: boolean
                type: object
              podEviction:
                default: {}
                description: podEviction configures eviction based resizing behavior.
                properties:
                  enabled:
                    default: true
                    description: enabled controls whether pod eviction based resizing
                      is allowed.
                    type: boolean
                  retryPodDisruptionBudget:
                    default: true
                    description: retryPodDisruptionBudget controls whether PDB rejections
                      should retry.
                    type: boolean
                type: object
              safetyChecks:
                default: {}
                description: safetyChecks configures safety guardrails.
                properties:
                  enableHpaFilter:
                    default: true
                    description: enableHpaFilter filters out resize actions for resources
                      targeted by an HPA.
                    type: boolean
                  enableLimitRangeFilter:
                    default: true
                    description: enableLimitRangeFilter filters out actions that violate
                      namespace LimitRanges.
                    type: boolean
                  enablePauseUntilAnnotationCheck:
                    default: true
                    description: enablePauseUntilAnnotationCheck blocks when pause-until
                      annotations are present.
                    type: boolean
                  enablePodLimitRangeFilter:
                    default: true
                    description: enablePodLimitRangeFilter filters out actions that
                      violate pod-level LimitRanges.
                    type: boolean
                  enableResourceQuotaFilter:
                    default: true
                    description: enableResourceQuotaFilter blocks when a resize would
                      violate resource quotas.
                    type: boolean
                  enableVpaFilter:
                    default: true
                    description: enableVpaFilter filters out resize actions for resources
                      actively managed by a VPA.
                    type: boolean
                  minCpuChangePercent:
                    default: 5
                    description: minCpuChangePercent filters out CPU resize actions
                      below the percent delta threshold.
                    minimum: 0
                    type: integer
                  minMemoryChangePercent:
                    default: 5
                    description: minMemoryChangePercent filters out memory resize
                      actions below the percent delta threshold.
                    minimum: 0
                    type: integer
                  minReadyDuration:
                    default: 10s
                    description: minReadyDuration blocks resizing until a pod has
                      been Ready for this long.
                    type: string
                  nodeCpuHeadroom:
                    default: 10%
                    description: nodeCpuHeadroom reduces node allocatable CPU by a
                      percentage or quantity before capacity checks.
                    type: string
                  nodeMemoryHeadroom:
                    default: 200Mi
                    description: nodeMemoryHeadroom reduces node allocatable memory
                      by a percentage or quantity before capacity checks.
                    type: string
                  requireNodeAllocatable:
                    default: true
                    description: requireNodeAllocatable filters out request increases
                      that exceed node allocatable capacity.
                    type: boolean
                  requireOwnerPodsReady:
                    default: false
                    description: requireOwnerPodsReady blocks resizing until all pods
                      under the owner are Ready.
                    type: boolean
                  resizeRetryInterval:
                    default: 30s
                    description: resizeRetryInterval controls the requeue interval
                      for blocked resizes.
                    type: string
                  respectWorkloadMaxUnavailable:
                    default: true
                    description: respectWorkloadMaxUnavailable blocks resizing if
                      it would exceed maxUnavailable.
                    type: boolean
                  retainGuaranteedQOS:
                    default: false
                    description: |-
                      retainGuaranteedQOS controls whether requests are kept equal to limits for Guaranteed QoS pods during resizing.
                      When enabled, limits are treated as the source of truth for cpu/memory and matching request actions are aligned.
                    type: boolean
                type: object
              scheduling:
                default: {}
                description: |-
                  scheduling configures inclusion and exclusion windows for resize actions.
                  The schedule applies to controller-driven in-place resize and pod eviction.
                  Admission-time pod creation ignores these windows and may still apply the desired resources immediately.
                properties:
                  exclusionWindows:
                    description: |-
                      exclusionWindows define when resizing is blocked.
                      Exclusion windows always take precedence over inclusion windows.
                    items:
                      properties:
                        end:
                          description: |-
                            end is the exclusive local time when the window ends, in 24-hour HH:MM format.
                            Defaults to 24:00 (end of day). 24:00 is only valid for end and represents the end of the current day.
                            When start is greater than end, the window is treated as spanning midnight.
                          maxLength: 5
                          pattern: ^$|^([01][0-9]|2[0-3]):[0-5][0-9]$|^24:00$
                          type: string
                        name:
                          description: name identifies the scheduling window in logs
                            and summaries.
                          maxLength: 128
                          type: string
                        start:
                          description: |-
                            start is the inclusive local time when the window begins, in 24-hour HH:MM format.
                            Defaults to 00:00 (start of day).
                          maxLength: 5
                          pattern: ^$|^([01][0-9]|2[0-3]):[0-5][0-9]$
                          type: string
                        timezone:
                          default: UTC
                          description: timezone is the IANA timezone used to interpret
                            weekdays, start, and end.
                          maxLength: 64
                          type: string
                        weekdays:
                          description: |-
                            weekdays restricts the window to specific days of the week.
                            Supported values are full or short weekday names such as Monday or Mon.
                            When omitted, the window applies every day.
                          items:
                            enum:
                            - Sunday
                            - Sun
                            - Monday
                            - Mon
                            - Tuesday
                            - Tue
                            - Tues
                            - Wednesday
                            - Wed
                            - Thursday
                            - Thu
                            - Thur
                            - Thurs
                            - Friday
                            - Fri
                            - Saturday
                            - Sat
                            type: string
                          maxItems: 7
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                      x-kubernetes-validations:
                      - message: start and end must not be the same
                        rule: '!has(self.start) || !has(self.end) || self.start !=
                          self.end'
                    maxItems: 32
                    type: array
                  inclusionWindows:
                    description: |-
                      inclusionWindows define when resizing is allowed.
                      When at least one inclusion window is present, resizing is allowed only while inside any inclusion window.
                    items:
                      properties:
                        end:
                          description: |-
                            end is the exclusive local time when the window ends, in 24-hour HH:MM format.
                            Defaults to 24:00 (end of day). 24:00 is only valid for end and represents the end of the current day.
                            When start is greater than end, the window is treated as spanning midnight.
                          maxLength: 5
                          pattern: ^$|^([01][0-9]|2[0-3]):[0-5][0-9]$|^24:00$
                          type: string
                        name:
                          description: name identifies the scheduling window in logs
                            and summaries.
                          maxLength: 128
                          type: string
                        start:
                          description: |-
                            start is the inclusive local time when the window begins, in 24-hour HH:MM format.
                            Defaults to 00:00 (start of day).
                          maxLength: 5
                          pattern: ^$|^([01][0-9]|2[0-3]):[0-5][0-9]$
                          type: string
                        timezone:
                          default: UTC
                          description: timezone is the IANA timezone used to interpret
                            weekdays, start, and end.
                          maxLength: 64
                          type: string
                        weekdays:
                          description: |-
                            weekdays restricts the window to specific days of the week.
                            Supported values are full or short weekday names such as Monday or Mon.
                            When omitted, the window applies every day.
                          items:
                            enum:
                            - Sunday
                            - Sun
                            - Monday
                            - Mon
                            - Tuesday
                            - Tue
                            - Tues
                            - Wednesday
                            - Wed
                            - Thursday
                            - Thu
                            - Thur
                            - Thurs
                            - Friday
                            - Fri
                            - Saturday
                            - Sat
                            type: string
                          maxItems: 7
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                      x-kubernetes-validations:
                      - message: start and end must not be the same
                        rule: '!has(self.start) || !has(self.end) || self.start !=
                          self.end'
                    maxItems: 32
                    type: array
                type: object
            type: object
          status:
            description: status defines the observed state of ClusterAutomationStrategy
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ClusterAutomationStrategy resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
