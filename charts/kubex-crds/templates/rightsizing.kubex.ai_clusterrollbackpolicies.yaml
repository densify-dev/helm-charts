---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clusterrollbackpolicies.rightsizing.kubex.ai
spec:
  group: rightsizing.kubex.ai
  names:
    kind: ClusterRollbackPolicy
    listKind: ClusterRollbackPolicyList
    plural: clusterrollbackpolicies
    singular: clusterrollbackpolicy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterRollbackPolicy is the Schema for the clusterrollbackpolicies
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterRollbackPolicy
            properties:
              adoptionThresholdPercent:
                description: adoptionThresholdPercent is the percentage of the workload
                  cohort that must adopt the target resources successfully.
                format: int32
                maximum: 100
                minimum: 1
                type: integer
              backoff:
                description: backoff controls how long the controller waits before
                  completing a rollback turn and how many turns are allowed.
                properties:
                  maxAttempts:
                    description: maxAttempts caps the number of turns before the controller
                      marks the rollback as permanently failed.
                    format: int32
                    minimum: 1
                    type: integer
                  multiplyByTurn:
                    description: |-
                      multiplyByTurn scales the base duration by the current turn number.
                      Example: with timePeriod 1m and multiplyByTurn 2, turn 1 waits 2m and turn 2 waits 4m.
                    format: int32
                    minimum: 1
                    type: integer
                  timePeriod:
                    description: timePeriod is the base duration for a rollback turn.
                    type: string
                required:
                - maxAttempts
                - multiplyByTurn
                - timePeriod
                type: object
              monitoringPeriod:
                description: monitoringPeriod bounds how long the controller observes
                  an active resize attempt before declaring failure.
                type: string
              rollbackTarget:
                description: rollbackTarget determines whether rollback returns to
                  manifest resources or the last known successful state.
                enum:
                - manifest
                - lastSuccessful
                type: string
              scope:
                description: scope narrows the workloads and namespaces this policy
                  applies to.
                properties:
                  labelSelector:
                    description: labelSelector limits the workload objects (e.g.,
                      Deployments, CronJobs) this policy applies to.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespaceSelector:
                    description: namespaceSelector restricts the namespaces this policy
                      applies to.
                    properties:
                      operator:
                        description: operator determines how the listed values are
                          evaluated.
                        enum:
                        - In
                        - NotIn
                        type: string
                      values:
                        description: values contains the namespace name patterns to
                          match.
                        items:
                          type: string
                        minItems: 1
                        type: array
                    required:
                    - operator
                    - values
                    type: object
                  workloadTypes:
                    default:
                    - Deployment
                    - StatefulSet
                    - CronJob
                    - Rollout
                    - Job
                    - AnalysisRun
                    - DaemonSet
                    - Model
                    description: workloadTypes limits the workload kinds this policy
                      applies to. When omitted, the default workload types are targeted.
                    items:
                      description: WorkloadType enumerates the workload kinds a policy
                        can target.
                      enum:
                      - Deployment
                      - StatefulSet
                      - DaemonSet
                      - CronJob
                      - Rollout
                      - Job
                      - AnalysisRun
                      - StrimziPodSet
                      - Model
                      type: string
                    type: array
                required:
                - namespaceSelector
                type: object
              weight:
                default: 0
                description: |-
                  weight determines which policy wins when multiple rollback policies of the same kind match.
                  Higher weights take precedence. When weights are equal, older policies win.
                format: int32
                minimum: 0
                type: integer
            required:
            - adoptionThresholdPercent
            - monitoringPeriod
            - rollbackTarget
            - scope
            type: object
          status:
            description: status defines the observed state of ClusterRollbackPolicy
            properties:
              activeTrackedWorkloads:
                description: activeTrackedWorkloads reports how many workloads currently
                  have an active rollback session for this policy.
                format: int32
                type: integer
              conditions:
                description: conditions represent the current state of the RollbackPolicy
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failedWorkloads:
                description: failedWorkloads reports how many workloads are currently
                  in terminal failedPermanent state for this policy.
                format: int32
                type: integer
              lastErrorSummary:
                description: lastErrorSummary surfaces the latest controller error
                  seen while reconciling this policy.
                type: string
              observedGeneration:
                description: observedGeneration is the latest generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
