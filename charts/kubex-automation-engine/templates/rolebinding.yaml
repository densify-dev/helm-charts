{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kubex-automation-engine.fullname" . }}-manager-rolebinding
  labels:
    {{- include "kubex-automation-engine.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubex-automation-engine.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "kubex-automation-engine.serviceAccountName" . }}
  namespace: {{ include "kubex-automation-engine.namespace" . }}
{{- end }}
