{{- if .Values.certmanager.enabled }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: kubex-selfsigned-cluster-issuer
  labels:
    {{- include "kubex-automation-controller.metadataLabels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}
